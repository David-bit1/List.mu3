# URL del backend (Express en Render). Sin barra final.
# En Vercel: Project Settings -> Environment Variables -> VITE_API_URL
# En desarrollo local puede quedar vacío y usar el proxy de Vite (BACKEND_URL).
VITE_API_URL=
