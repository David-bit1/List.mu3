{
  "name": "iptv-catalog-admin",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo: frontend (Vite/Vercel) + backend (Express/Render) para catálogo multimedia y generador M3U.",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev:backend": "npm -w backend run dev",
    "dev:frontend": "npm -w frontend run dev",
    "build:backend": "npm -w backend run build",
    "build:frontend": "npm -w frontend run build"
  }
}
